import React from 'react';

const Layout = ({ children }) => {
    return (
        <div>
            {/* Add your layout structure here */}
            {children}
        </div>
    );
};

export default Layout;
